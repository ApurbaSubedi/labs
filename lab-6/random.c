//wap to ask m x n and p x q order matrix, check if addition is possible or not. If possible, find sum.
 //#include <stdio.h>
 //void main()
   //{
      // int a[10][10],b[10][10],m,n,p,q,sum[10][10],i,j,k,l;
      // printf("Enter the no. of row of 1st matrix:");
      // scanf("%d", &m);
      // printf("Enter the no. of column of 1st matrix:");
      // scanf("%d", &n);
     // printf("Enter the no. of row of 2nd matrix:");
     // scanf("%d", &p);
     // printf("Enter the no. of column of 2nd matrix:");
     // scanf("%d", &q);
  
     // if (m!=p && n!=q)
     // {
       //   printf("Addition is not possible.");
     // }
     // else {
        //  printf("Enter elements of the 1st matrix:\n");
      // for(i=1; i<=m; i++)
      // {
         // for(j=1; j<=n; j++)
            //  {
            //      printf("Element a[%d][%d] = ",i,j);
          //        scanf("\t%d",&a[i][j]);
        //      }
      // }
      //  printf("Enter elements of the 2nd matrix:\n");
      // for(i=1; i<=p; i++)
      // {
         // for(j=1; j<=q; j++)
             // {
            //      printf("Element b[%d][%d] = ",i,j);
          //        scanf("\t%d",&b[i][j]);
        //      }
      // }
      // printf("The sum of matrix is:\n");
    // for(i=1; i<=m; i++)
    // {
        // for(j=1; j<=n; j++)
             //{
            //    sum[i][j]= a[i][j] + b[i][j]; 
         //      printf("%d\t",sum[i][j]);
        //     }
      //       printf("\n");
   // }
   //  }
 
 //}
 
  //wap to ask n X n order square matrix and find the sum of all the diagonals element of that matrix.
  //#include<stdio.h>
  //void main()
  //{
     // int a[10][10],n, i, sum=0, j;
     // printf("Enter the order of matrix:");
     // scanf("%d", &n);
     // printf("Enter elements of the matrix:\n");
  
      //for(i=1; i<=n; i++)
      //{
         //for(j=1; j<=n; j++)
              //{
            //      printf("Element a[%d][%d] = ",i,j);
         //        scanf("\t%d",&a[i][j]);68       //      }
      //}
      //printf("The matrix is:\n");
      //for (i=1;i<=n;i++)
     //{
          //for (j=1;j<=n;j++)
         // {
           //   printf("%d\t", a[i][j]);
         // }
        //  printf("\n");
      //}
      //for (i=1;i<=n;i++)
     // {
       //   for (j=1;j<=n;j++)
          //{
  
             // if (i==j || i+j==n-1)
            // {
           //   sum = sum+ a[i][j];
         //     }
       //   }
     // }
    //  printf("Sum of the diagonal elements= %d", sum);
  
  //}
  
 
 
  //wap to ask m by n order matrix. find the transpose of that matrix and display the transpose of that matrix
 
